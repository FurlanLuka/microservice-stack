{
  "name": "nest-modules-health",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/nest-modules/health/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/nest-modules/health",
        "tsConfig": "libs/nest-modules/health/tsconfig.lib.json",
        "packageJson": "libs/nest-modules/health/package.json",
        "main": "libs/nest-modules/health/src/index.ts",
        "assets": ["libs/nest-modules/health/*.md"]
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/publish.mjs nest-modules-health {args.ver} {args.tag}"
      },
      "dependsOn": ["build"]
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/nest-modules/health/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/nest-modules/health/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
