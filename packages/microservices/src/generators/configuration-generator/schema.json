{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "ConfigurationGeneratorPlugin",
  "title": "",
  "type": "object",
  "properties": {
    "organisationName": {
      "type": "string",
      "description": "What is the name of your organisation?",
      "x-prompt": "What is the name of your organisation?"
    },
    "deploymentConfigurationEnabled": {
      "type": "boolean",
      "description": "Enable deployment configuration?",
      "x-prompt": "Enable deployment configuration?"
    },
    "includeQueue": {
      "type": "boolean",
      "description": "Enable queue configuration? (only with deployment enabled)",
      "x-prompt": "Enable queue configuration? (only with deployment enabled)"
    },
    "includeRedis": {
      "type": "boolean",
      "description": "Enable redis configuration? (only with deployment enabled)",
      "x-prompt": "Enable redis configuration? (only with deployment enabled)"
    },
    "includePostgres": {
      "type": "boolean",
      "description": "Enable postgres configuration? (only with deployment enabled)",
      "x-prompt": "Enable postgres configuration? (only with deployment enabled)"
    }
  },
  "required": ["organisationName", "deploymentConfigurationEnabled"]
}
