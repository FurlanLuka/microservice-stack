{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "ServiceDeploymentGeneratorSchema",
  "title": "",
  "type": "object",
  "properties": {
    "applicationName": {
      "description": "The application project to target",
      "type": "string",
      "$default": {
        "$source": "projectName"
      },
      "x-prompt": "What apllication would you like to target?"
    }
  },
  "required": ["applicationName"]
}
